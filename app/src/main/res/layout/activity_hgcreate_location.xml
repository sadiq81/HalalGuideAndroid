<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="dk.eazyit.halalguide.ui.create_location.HGCreateLocationActivity">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/pictures"

        android:layout_width="100dp"
        android:layout_height="200dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />

    <EditText
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/pictures"
        android:layout_toRightOf="@+id/pictures"
        android:text="Name" />

    <EditText
        android:id="@+id/address_road"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/name"
        android:layout_toEndOf="@+id/pictures"
        android:layout_toLeftOf="@+id/address_road_number"
        android:layout_toRightOf="@+id/pictures"
        android:layout_toStartOf="@+id/address_road_number"
        android:maxLines="1"
        android:text="Road" />

    <EditText
        android:id="@+id/address_road_number"
        android:layout_width="50dp"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/name"
        android:maxLines="1"
        android:text="10" />

</RelativeLayout>
